import os
with open("syslog.log", "r") as f:
	for line in f:
		error_is_none = re.search(r': ERROR', line)
		info_is_none = re.search(r': INFO', line)
		if error_is_none is not None:
                error = re.search(r'ERROR ([\w ]*)', line)[1].strip()
                if error not in error_types:
                    error_types[error] = 1
                else:
                    error_types[error] += 1
wq
quit()
